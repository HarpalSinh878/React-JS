import 'package:flutter/material.dart';

class EarningTabspage extends StatefulWidget {
  @override
  EarningTabspageState createState() => EarningTabspageState();
}

class EarningTabspageState extends State<EarningTabspage> {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Earning tab page",style: TextStyle(color: Colors.black),),
    );
  }
}