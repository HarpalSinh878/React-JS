import 'package:flutter/material.dart';

class RatingTabspage extends StatefulWidget {
  @override
  RatingTabspageState createState() => RatingTabspageState();
}

class RatingTabspageState extends State<RatingTabspage> {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Rating tab page",style: TextStyle(color: Colors.black),),
    );
  }
}